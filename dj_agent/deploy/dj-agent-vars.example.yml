# Encrypt this file with ansible-vault before using it in a real deployment.
dj_agent_backend_grpc_addr: "autoadmin.example.com:9001"
dj_agent_backend_token: "replace-with-a-vault-secret"
dj_agent_run_user: root
# 必填：实例名（须与 frontend 创建主机时填的 instance_name 保持一致）。
dj_agent_instance_name: "host-001"
